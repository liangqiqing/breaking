1.对象的向上转型，其实就是多态写法：
格式：父类名称  对象名 = new 子类名称；
含义：右则创建一个子类对象，把它当做父类来看待使用。
注意事项：向上转型一定是安全的。

2.对象的向下转型，其实就是一个【还原】的动作：
格式： 子类名称 对象名 = （子类对象） 父类对象；
含义：将父类对象，【还原】成为本类的子类对象。
注意事项：
a.必须保证对象本来创建的时候，原来是什么现在也是什么，才能向下转型。
b.如果对象创建的时候不是本来的，现在非要向下转型，就会报错。
XlassCastException